name: 🛠️ Build Kernel

on:
  workflow_dispatch:
    inputs:
      kernel_version:
        description: "内核版本号 (如: 6.18)"
        required: true
        default: "6.18"

jobs:
  build:
    name: "构建内核 v${{ github.event.inputs.kernel_version || '6.18' }}"
    runs-on: ubuntu-24.04-arm

    steps:
      - name: "📥 检出代码"
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: "💾 设置ccache缓存 (先于构建工具)"
        uses: actions/cache@v4
        id: cache-ccache
        with:
          path: /home/runner/.ccache
          # 修复1: 使用固定的key结构，移除脚本哈希（脚本可能频繁变化）
          key: ${{ runner.os }}-ccache-kernel-${{ github.event.inputs.kernel_version || '6.18' }}-${{ github.ref_name }}-${{ github.run_id }}
          # 修复2: 简化restore-keys，确保能找到历史缓存
          restore-keys: |
            ${{ runner.os }}-ccache-kernel-${{ github.event.inputs.kernel_version || '6.18' }}-${{ github.ref_name }}-
            ${{ runner.os }}-ccache-kernel-${{ github.event.inputs.kernel_version || '6.18' }}-
            ${{ runner.os }}-ccache-kernel-

      - name: "🔧 设置构建环境"
        run: |
          echo "⚙️ 正在安装必要的构建工具..."
          sudo apt update
          sudo apt install -y build-essential flex bison libssl-dev libelf-dev bc ccache
          echo "✅ 构建工具安装完成"
          
          echo "📦 检查ccache状态..."
          ccache --version
          
          # 确保ccache目录存在（无论缓存是否命中）
          sudo mkdir -p /home/runner/.ccache
          sudo chown -R runner:runner /home/runner/.ccache
          sudo chmod -R 777 /home/runner/.ccache
          
          # 配置ccache
          ccache --set-config=max_size=10G
          ccache --set-config=cache_dir=/home/runner/.ccache
          
          echo "📊 缓存恢复状态: ${{ steps.cache-ccache.outputs.cache-hit }}"
          ccache --show-stats

      - name: "🔨 编译内核"
        id: build
        run: |
          echo "🚀 开始编译内核版本 ${{ github.event.inputs.kernel_version || '6.18' }}"
          echo "💾 缓存状态: ${{ steps.cache-ccache.outputs.cache-hit }}"
          
          echo "📊 编译前ccache统计:"
          ccache --show-stats
          
          echo "⚙️ 执行编译脚本..."
          # 设置ccache环境变量，确保编译过程使用ccache
          export CCACHE_DIR=/home/runner/.ccache
          export CCACHE_MAXSIZE=10G
          export PATH="/usr/lib/ccache:$PATH"
          
          # 检查是否实际使用了ccache
          echo "检查编译器是否使用ccache包装:"
          which gcc
          gcc --version | head -1
          
          # 执行编译
          sudo bash raphael-kernel_build.sh "${{ github.event.inputs.kernel_version || '6.18' }}"
          
          echo "✅ 内核编译完成"
          echo "📊 编译后ccache统计:"
          ccache --show-stats
          echo "缓存命中率详情:"
          ccache --show-stats --verbose | grep -E "(hit rate|cache hit|cache miss)"

      - name: "💾 保存ccache缓存"
        # 无论编译是否成功都保存缓存，确保下次构建能利用已有缓存
        if: always()
        uses: actions/cache/save@v4
        with:
          path: /home/runner/.ccache
          key: ${{ steps.cache-ccache.outputs.cache-primary-key }}

      - name: "📦 收集编译产物"
        id: collect_artifacts
        run: |
          echo "🔍 查找编译产物..."
          find . -name "*.deb" -type f | head -20
          
          echo "📁 编译产物列表:"
          ls -la *.deb 2>/dev/null || echo "⚠️  未找到 .deb 文件"
          echo ""
          echo "📊 文件大小统计:"
          du -h *.deb 2>/dev/null || echo "暂无产物文件"
          
          # 统计包数量并输出，供后续步骤使用
          PKG_COUNT=$(ls -1 *.deb 2>/dev/null | wc -l)
          echo "📦 共找到 $PKG_COUNT 个包文件"
          echo "pkg_count=$PKG_COUNT" >> $GITHUB_OUTPUT

      - name: "📤 上传构建产物"
        uses: actions/upload-artifact@v4
        with:
          name: kernel-${{ github.event.inputs.kernel_version || '6.18' }}
          path: "*.deb"
          retention-days: 7
          if-no-files-found: error

      - name: "🏷️ 创建GitHub Release"
        if: steps.collect_artifacts.outputs.pkg_count > 0 && github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: kernel-${{ github.event.inputs.kernel_version || '6.18' }}
          name: "Kernel ${{ github.event.inputs.kernel_version || '6.18' }}"
          body: |
            ## 内核版本 ${{ github.event.inputs.kernel_version || '6.18' }}
            
            📅 构建时间: ${{ github.event.created_at }}
            🔖 内核版本: ${{ github.event.inputs.kernel_version || '6.18' }}
            
            ⚙️ 包含以下包:
            - linux-xiaomi-raphael_*.deb
            - firmware-xiaomi-raphael_*.deb  
            - alsa-xiaomi-raphael_*.deb
            
            🐛 自动构建于 GitHub Actions
          draft: false
          prerelease: false
          files: "*.deb"
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: "📋 构建总结"
        if: always()
        run: |
          echo "📊 ======== 构建总结 ========"
          echo "🏗️  工作流: ${{ github.workflow }}"
          echo "🎯 目标版本: ${{ github.event.inputs.kernel_version || '6.18' }}"
          echo "💾 缓存命中: ${{ steps.cache-ccache.outputs.cache-hit }}"
          echo "📁 产物数量: ${{ steps.collect_artifacts.outputs.pkg_count || 0 }}"
          echo "⏱️  运行状态: ${{ job.status }}"
          echo "==========================="